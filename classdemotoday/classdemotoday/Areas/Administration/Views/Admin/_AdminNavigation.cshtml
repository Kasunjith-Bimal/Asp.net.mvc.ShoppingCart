@{
    Layout = null;
}

<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">360x</a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active">@Ajax.ActionLink("Active Shop", "ActiveShop", "Admin", new AjaxOptions
                          { 
                          UpdateTargetId="targetDiv",
                          InsertionMode = InsertionMode.Replace, // <-- Replace the content of DOM element
                          HttpMethod = "GET",
                          
                          })</li>
            <li>@Ajax.ActionLink("Home", "Index", "Home",new{area =""}, new AjaxOptions
                          {
                              UpdateTargetId = "targetDiv",
                              InsertionMode = InsertionMode.Replace, // <-- Replace the content of DOM element
                              HttpMethod = "GET",

                          })</li>
            <li>@Ajax.ActionLink("Pending Active Shop", "PendingActiveShop", "Admin", new AjaxOptions
           {
               UpdateTargetId = "targetDiv",
               InsertionMode = InsertionMode.Replace, // <-- Replace the content of DOM element
               HttpMethod = "GET",

           })</li>
            <li><a href="#">Page 2</a></li>
        </ul>
        <form class="navbar-form navbar-left">
            <div class="input-group">
                <input type="text" class="form-control" placeholder="Search" name="Search">
                <div class="input-group-btn">
                    <button class="btn btn-default" type="submit">
                        <i class="glyphicon glyphicon-search"></i> Search Shop
                    </button>
                </div>
            </div>
        </form>
    </div>
</nav>
